# CI/CD and Code Merge
This project makes use of `pytest` to run test verifications.

Run `make verify` for unit and end to end testings. If you find error, warning or the command exited with non-zero exit code, you must report it back. You are not responsible for fixing the error. You do run, test, verify and report.
